package com.zeno.quanxueclient.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.zeno.quanxueclient.bean.BookContentBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "BOOK_CONTENT_BEAN".
*/
public class BookContentBeanDao extends AbstractDao<BookContentBean, Void> {

    public static final String TABLENAME = "BOOK_CONTENT_BEAN";

    /**
     * Properties of entity BookContentBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property SectionUrl = new Property(0, String.class, "sectionUrl", false, "SECTION_URL");
        public final static Property Content = new Property(1, String.class, "content", false, "CONTENT");
    }


    public BookContentBeanDao(DaoConfig config) {
        super(config);
    }
    
    public BookContentBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"BOOK_CONTENT_BEAN\" (" + //
                "\"SECTION_URL\" TEXT," + // 0: sectionUrl
                "\"CONTENT\" TEXT);"); // 1: content
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"BOOK_CONTENT_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, BookContentBean entity) {
        stmt.clearBindings();
 
        String sectionUrl = entity.getSectionUrl();
        if (sectionUrl != null) {
            stmt.bindString(1, sectionUrl);
        }
 
        String content = entity.getContent();
        if (content != null) {
            stmt.bindString(2, content);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, BookContentBean entity) {
        stmt.clearBindings();
 
        String sectionUrl = entity.getSectionUrl();
        if (sectionUrl != null) {
            stmt.bindString(1, sectionUrl);
        }
 
        String content = entity.getContent();
        if (content != null) {
            stmt.bindString(2, content);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public BookContentBean readEntity(Cursor cursor, int offset) {
        BookContentBean entity = new BookContentBean( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // sectionUrl
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // content
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, BookContentBean entity, int offset) {
        entity.setSectionUrl(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setContent(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(BookContentBean entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(BookContentBean entity) {
        return null;
    }

    @Override
    public boolean hasKey(BookContentBean entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
